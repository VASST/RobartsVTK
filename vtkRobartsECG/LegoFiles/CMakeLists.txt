PROJECT (VTKLEGO)

INCLUDE_DIRECTORIES(${USB_INCLUDE_DIR})

SET (legousb_SRCS
  NXT_USB.cxx
  NXT_USB_linux.cxx
)

ADD_LIBRARY(vtklego ${legousb_SRCS} )

TARGET_LINK_LIBRARIES (vtklego ${USB_LIBRARIES} )
